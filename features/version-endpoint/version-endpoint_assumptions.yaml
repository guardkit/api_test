# Assumptions manifest for version-endpoint.feature
# Generated by /feature-spec

feature: "Version Endpoint"
generated: "2026-07-09T14:32:00Z"
stack: "generic"
review_required: true

assumptions:
  - id: "ASSUM-001"
    scenario: "The git commit hash returned is a short 7-character string"
    assumption: "The git commit hash returned is a short (7-character) hash, not a full SHA"
    confidence: low
    basis: "Open question in input: short vs. full SHA; defaulting to short hash as common convention for /version endpoints"
    human_response: "deferred"

  - id: "ASSUM-002"
    scenario: "The /version response has a flat JSON structure with no nesting"
    assumption: "The /version response is a flat JSON object with no nested structures, matching the shape of /uptime and /stats"
    confidence: low
    basis: "Open question in input: response shape convention; defaulting to flat JSON matching existing endpoints"
    human_response: "deferred"

  - id: "ASSUM-003"
    scenario: "The JSON response uses the keys "version", "commit", and "service""
    assumption: "The JSON keys are named exactly "version", "commit", and "service" (lowercase, no prefixes like "app_" or "git_")"
    confidence: low
    basis: "Open question in input: key naming convention; defaulting to short lowercase keys without prefixes"
    human_response: "deferred"