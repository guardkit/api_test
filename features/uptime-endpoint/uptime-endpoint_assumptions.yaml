# Assumptions manifest for uptime-endpoint.feature
# Generated by /feature-spec

feature: "Service Uptime Endpoint"
generated: "2026-07-12T06:57:14Z"
stack: "python"
review_required: false

assumptions:
  - id: "ASSUM-001"
    scenario: "Requesting uptime returns the service identity and running time"
    assumption: "The PO document's additional behaviours (503-when-degraded, version information field, 100ms latency budget, uptime-api-spec.md citation) are OUT of scope; the endpoint returns exactly the three requested fields"
    confidence: high
    basis: "The originating request names exactly three fields (service, started_at, uptime_seconds), the health-module structure, and no database access; the PO extras are template-bleed (known POCONTENT residual, named in the Factory-1 record)"
    human_response: "confirmed"

  - id: "ASSUM-002"
    scenario: "Uptime increases between consecutive requests"
    assumption: "started_at means application process start, captured once at app startup (not machine boot, not per-request)"
    confidence: medium
    basis: "'Process start time' in the request plus FastAPI lifespan convention"
    human_response: "confirmed"

  - id: "ASSUM-003"
    scenario: "Requesting uptime returns the service identity and running time"
    assumption: "uptime_seconds is a float with sub-second precision; no rounding contract"
    confidence: high
    basis: "The request states float explicitly"
    human_response: "confirmed"
