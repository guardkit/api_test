# Feature Spec Input: 0739c6f9-cd69-4f2b-b99f-7581382cb9e9

**Originator**: U03QR8WKT29

**Request**: Please add a /version endpoint to the api_test service. It should return JSON with three fields: the application version string, the git commit the running build was made from, and the service name. Read-only, no auth, no database — the same shape as the existing /uptime and /stats endpoints. Success: GET /version returns 200 with all three fields populated, and any write method on /version (POST/PUT/PATCH/DELETE) returns 405.

## Product Documentation

**project_name**: api_test /version endpoint

**mode**: greenfield

**epics**: [{'id': 'EPIC-001', 'name': 'Service Metadata Endpoint', 'bounded_context': 'Service Health and Metadata', 'description': 'Add a read-only /version endpoint to the api_test service that exposes application version, git commit, and service name, consistent with the existing /uptime and /stats endpoints.', 'features': [{'feature_id': 'FEAT-PO-001', 'title': 'GET /version returns application metadata', 'description': 'The api_test service exposes a GET /version endpoint that returns a JSON response with three fields: the application version string, the git commit hash from which the current build was produced, and the service name. On a successful GET request the endpoint returns HTTP 200 with all three fields populated from build-time-injected values. Any write method (POST, PUT, PATCH, DELETE) targeting /version must return HTTP 405 Method Not Allowed, enforcing the read-only contract of the endpoint.', 'bounded_context': 'Service Health and Metadata', 'source_documents': ['request:GET /version returns 200 with all three fields populated', 'request:any write method on /version (POST/PUT/PATCH/DELETE) returns 405', 'request:return JSON with three fields: the application version string, the git commit the running build was made from, and the service name', 'request:Read-only, no auth, no database — the same shape as the existing /uptime and /stats endpoints'], 'constraints': ['Read-only endpoint — no auth required', 'No database access', 'Response must include all three fields: version, commit, and service name', 'Non-GET methods must return 405', 'Same response shape as existing /uptime and /stats endpoints'], 'suggested_context_files': [], 'depends_on': [], 'type': None, 'role': None, 'priority': None, 'moscow': None, 'value': None, 'complexity': None, 'acceptance_criteria': [], 'technical_notes': [], 'risks': [], 'open_questions': [], 'links': [], 'field_citations': {}}], 'source_documents': [], 'field_citations': {}}]

**priority_rationale**: This is a single, self-contained feature with no dependencies on other features. It is a straightforward addition to the api_test service and can be implemented immediately alongside the existing /uptime and /stats endpoints. There are no ordering concerns — this is the only epic and feature.

**constraints_and_dependencies**: ['No database access — values must come from build-time-injected configuration', 'Must follow the same JSON response shape and routing pattern as existing /uptime and /stats endpoints', 'Framework must support method-level 405 handling for the /version route']

**open_questions**: ["What is the exact JSON key naming convention for the three response fields? (e.g., 'version', 'commit', 'service' vs. 'app_version', 'git_commit', 'service_name')", 'What format should the git commit hash be in — full SHA or short (7-character) hash?', 'How are the version string and git commit currently injected into the api_test service at build time? Is there an existing mechanism to read them at runtime?']

**feature_spec_inputs**: [{'feature_id': 'FEAT-PO-001', 'title': 'GET /version returns application metadata', 'description': 'The api_test service exposes a GET /version endpoint that returns a JSON response with three fields: the application version string, the git commit hash from which the current build was produced, and the service name. On a successful GET request the endpoint returns HTTP 200 with all three fields populated from build-time-injected values. Any write method (POST, PUT, PATCH, DELETE) targeting /version must return HTTP 405 Method Not Allowed, enforcing the read-only contract of the endpoint.', 'bounded_context': 'Service Health and Metadata', 'source_documents': ['request:GET /version returns 200 with all three fields populated', 'request:any write method on /version (POST/PUT/PATCH/DELETE) returns 405', 'request:return JSON with three fields: the application version string, the git commit the running build was made from, and the service name', 'request:Read-only, no auth, no database — the same shape as the existing /uptime and /stats endpoints'], 'constraints': ['Read-only endpoint — no auth required', 'No database access', 'Response must include all three fields: version, commit, and service name', 'Non-GET methods must return 405', 'Same response shape as existing /uptime and /stats endpoints'], 'suggested_context_files': [], 'depends_on': [], 'type': None, 'role': None, 'priority': None, 'moscow': None, 'value': None, 'complexity': None, 'acceptance_criteria': [], 'technical_notes': [], 'risks': [], 'open_questions': [], 'links': [], 'field_citations': {}}]

**change_summary**: None

**coverage_score**: None

**source_documents**: []

**assumptions**: [{'id': 'ASM-001', 'category': 'technical', 'statement': 'The application version string and git commit hash are available as runtime-accessible values, injected at build time (e.g., via environment variables, build-time templating, or a generated metadata file).', 'source': 'problem statement', 'confidence': 'high', 'impact_if_wrong': 'If version and commit are not accessible at runtime, the endpoint would need a build pipeline change before it can be implemented.'}, {'id': 'ASM-002', 'category': 'domain', 'statement': "The service name is 'api_test' and is a static, known value used consistently across the service.", 'source': 'problem statement', 'confidence': 'high', 'impact_if_wrong': 'If the service name is dynamically determined or uses a different convention, the response value may need adjustment.'}, {'id': 'ASM-003', 'category': 'technical', 'statement': 'The existing /uptime and /stats endpoints follow a consistent JSON response pattern (flat JSON body, no nesting) that the /version endpoint should mirror.', 'source': 'problem statement', 'confidence': 'medium', 'impact_if_wrong': 'If the existing endpoints differ in shape, the /version endpoint may need to diverge or a standardisation decision may be required.'}, {'id': 'ASM-004', 'category': 'technical', 'statement': 'The web framework used by api_test supports returning 405 Method Not Allowed for specific HTTP methods on a route without requiring explicit handler code for each write method.', 'source': 'problem statement', 'confidence': 'medium', 'impact_if_wrong': 'If the framework does not auto-return 405, explicit handlers or middleware for POST/PUT/PATCH/DELETE on /version may be needed.'}]

**estimate_unit**: None

