id: FEAT-UBEM
name: "GET /users/by-email endpoint"
description: 'Look up a user by email at GET /users/by-email?email=<EmailStr> ->
  200 UserPublic | 404 no match | 422 malformed address | 503 DB-down naming the
  cause (the /users/count convention). REUSES the existing crud.get_user_by_email
  (no new query); declared before the by-id route (the FEAT-UCNT route-order
  precedent). The third forge e2e run''s demo feature — the clean-routine-datum
  candidate: deliberately thin, the chain is under test. Gherkin + assumptions in
  features/users-by-email-endpoint/ (assumptions PENDING Rich''s review).'
created: '2026-07-27T17:00:00.000000'
status: planned
complexity: 2
estimated_tasks: 1
tasks:
- id: TASK-UBEM-001
  name: Add GET /users/by-email endpoint
  file_path: tasks/backlog/users-by-email-endpoint/TASK-UBEM-001-add-users-by-email-endpoint.md
  complexity: 2
  dependencies: []
  status: pending
  implementation_mode: task-work
  estimated_minutes: 30
  requires_infrastructure: []
orchestration:
  parallel_groups:
  - - TASK-UBEM-001
  estimated_duration_minutes: 30
  recommended_parallel: 1
preflight_strict: false
evidence_repos: []
