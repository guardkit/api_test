id: FEAT-UCNT
name: "GET /users/count endpoint"
description: 'Expose the stored-user total at GET /users/count returning exactly
  {"count": <non-negative int>}. Extends the existing users router (declared before
  the by-id route so neither shadows the other). Data-bearing: the count reads the
  real users table, DB-down degrades honestly to 503 naming the cause. The second
  forge end-to-end run''s demo feature: FEAT-UPT1-class size, one honest step more
  real. Gherkin + assumptions in features/users-count-endpoint/ (all four assumptions
  CONFIRMED by Rich 2026-07-27).'
created: '2026-07-26T20:30:00.000000'
status: planned
complexity: 3
estimated_tasks: 1
tasks:
- id: TASK-UCNT-001
  name: Add GET /users/count endpoint
  file_path: tasks/backlog/users-count-endpoint/TASK-UCNT-001-add-users-count-endpoint.md
  complexity: 3
  dependencies: []
  status: pending
  implementation_mode: task-work
  estimated_minutes: 35
  requires_infrastructure: []
orchestration:
  parallel_groups:
  - - TASK-UCNT-001
  estimated_duration_minutes: 35
  recommended_parallel: 1
preflight_strict: false
evidence_repos: []
